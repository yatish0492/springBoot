package com.yatish.springBootStart;

public class S14_5_AspectJ {

	/*
	 * Spring AOP supports defining aspects on only methods using pointcuts. were as AspectJ supports defining aspects on class fields/properties as well itseems. like we can define an
	 * aspect on a field of a class so that whenever the field value is being changed, the aspect will be called. but spring AOP doesnt support aspects on class fields/proeprties. 
	 * 
	 * Need to learn AspectJ?
	 * no need as of now. will see later.
	 * 
	 * 
	 */

}
