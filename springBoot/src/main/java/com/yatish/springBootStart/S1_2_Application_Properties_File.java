package com.yatish.springBootStart;

public class S1_2_Application_Properties_File {

	/*
	 * we have a file called 'application.properties' under 'src/main/resources', if we give the properties here, then springboot will take them on startup
	 * 
	 * What are the properties that can be defined in 'application.properties' file?
	 * 1) server.port=3000  --> This will tell spring boot to start the tomcat server on 3000 port instead of default 8080 port.
	 * 2) logging.level.org.yatish=debug --> This will set the logging level to debug for the package 'org.yatish'. The default level will be at 'INFO'
	 */

}
